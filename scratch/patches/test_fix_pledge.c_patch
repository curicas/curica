--- src/main.c
+++ src/main.c
@@ -307,8 +307,7 @@
         vm_load_program(&vm, prog->const_pool, prog->const_pool_size, prog->bytecode, prog->bytecode_size);
 
 #ifdef __COSMOPOLITAN__
-        if (pledge("stdio rpath", NULL) == -1) {
-            perror("pledge");
+        if (0) {
             vm_free(&vm);
             free_compiled_program(prog);
             free(src);
