# Arrow Functions

**Category**: Parser_and_Lexer
**Status**: Completed

## Description

Add `TOK_ARROW` (`=>`). Parse single parameter, parenthesised parameters with concise/block body. Add `is_arrow` flag to `JSFunction` to capture lexical `this`.
