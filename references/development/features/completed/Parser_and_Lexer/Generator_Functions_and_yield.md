# Generator Functions and yield

**Category**: Parser_and_Lexer
**Status**: Completed

## Description

Add `TOK_YIELD`. Add `is_generator` flag. Use `ucontext_t` to suspend/resume generator coroutines via `OP_YIELD`.
