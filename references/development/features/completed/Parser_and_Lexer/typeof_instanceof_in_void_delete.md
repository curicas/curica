# typeof instanceof in void delete

**Category**: Parser_and_Lexer
**Status**: Completed

## Description

Wire up `typeof`, `instanceof`, `in`, `void`, and `delete` to parser rules. Add `OP_DELETE_PROP` opcode.
