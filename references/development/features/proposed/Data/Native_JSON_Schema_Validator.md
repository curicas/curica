# Native JSON Schema Validator

**State**: Proposed
**Difficulty**: Medium
A C-level implementation of the JSON Schema validation spec. Instantly parses and strictly validates enormous HTTP request payloads significantly faster than Ajv or Zod running in JavaScript user-land.
