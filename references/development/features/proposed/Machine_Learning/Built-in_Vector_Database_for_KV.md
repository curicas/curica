# Built-in Vector Database for KV

**State**: Proposed
**Difficulty**: High
Extending the native `Curica.KV` persistent storage module to support high-dimensional vector embeddings. Writing native cosine-similarity search algorithms creates a zero-configuration backbone for RAG AI pipelines.
