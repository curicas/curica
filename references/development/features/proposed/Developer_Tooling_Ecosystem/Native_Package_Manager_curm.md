# Native Package Manager (`curm`)

**State**: Proposed
**Difficulty**: High
Implementing a package manager natively in C. It instantaneously parses `package.json` files and resolves dependency graphs utilizing POSIX memory-mapped I/O and zero-copy JSON parsing.
