# Native Unit Testing Framework

**State**: Proposed
**Difficulty**: Medium
Extending the built-in test runner to include a comprehensive suite of utilities mimicking `jest`. This includes deep mock generation, function spying, and snapshot testing natively enforced at the VM layer.
