# Real-Time Streaming Protocol (RTSP) Client

**State**: Proposed
**Difficulty**: Medium
A native C-level RTSP and RTP parser. Allows the runtime to connect to thousands of IP security cameras concurrently, capturing H.264 video frames natively without spawning heavy `ffmpeg` child processes.
