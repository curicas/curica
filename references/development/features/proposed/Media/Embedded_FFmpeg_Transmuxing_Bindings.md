# Embedded FFmpeg Transmuxing Bindings

**State**: Proposed
**Difficulty**: Extreme
Statically linking a stripped-down `libavformat` and `libavcodec`. Gives Curica absolute native power to slice, convert, and mux video streams purely through the `Curica.Media` module.
