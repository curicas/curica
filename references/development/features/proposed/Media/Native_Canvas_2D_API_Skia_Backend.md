# Native Canvas 2D API (Skia Backend)

**State**: Proposed
**Difficulty**: Extreme
Embedding the Skia graphics library to provide a fully spec-compliant HTML5 `<canvas>` API context. Empowers backend servers to render dynamic charts and image overlays incredibly fast entirely headless.
