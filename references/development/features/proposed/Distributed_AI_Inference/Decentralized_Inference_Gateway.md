# Decentralized Inference Gateway

**State**: Proposed
**Difficulty**: Medium
A specialized load balancer built into the networking stack that automatically routes inference requests (like prompt completions) to the least-utilized Curica node in the cluster holding the target model.
