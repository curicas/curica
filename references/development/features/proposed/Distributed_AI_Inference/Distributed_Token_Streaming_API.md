# Distributed Token Streaming API

**State**: Proposed
**Difficulty**: Medium
A built-in Server-Sent Events (SSE) optimization specifically designed to stream generated LLM tokens back to clients across a distributed cluster with absolute minimum latency.
