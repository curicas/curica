# Advanced TLS 1.3 Cipher Suite Management

**State**: Proposed
**Difficulty**: Medium
Exposing incredibly fine-grained, low-level control over OpenSSL/BoringSSL cipher suites directly to JavaScript, ensuring highly secure military-grade connection parameters for the `net` and `https` modules.
