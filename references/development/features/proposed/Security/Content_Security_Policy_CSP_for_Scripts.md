# Content Security Policy (CSP) for Scripts

**State**: Proposed
**Difficulty**: Medium
Bringing browser-level security policies to the backend. Enforcing strict CSP hashes on dynamic execution paradigms like `eval` and `new Function` directly inside the AST parser, eliminating injection attacks.
