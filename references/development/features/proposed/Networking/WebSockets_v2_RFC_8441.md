# WebSockets v2 (RFC 8441)

**State**: Proposed
**Difficulty**: High
Bootstrapping WebSocket streams natively over HTTP/2. This allows multiple parallel websocket streams to operate securely over a single multiplexed TCP connection directly within Curica's event loop.
