# Native DNS over HTTPS (DoH) Resolver

**State**: Proposed
**Difficulty**: Medium
Bypassing the host OS DNS resolver entirely to perform highly secure, encrypted DNS lookups directly through Cloudflare or Google DoH APIs inside the native event loop.
