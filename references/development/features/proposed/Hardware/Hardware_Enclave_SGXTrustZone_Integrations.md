# Hardware Enclave (SGX/TrustZone) Integrations

**State**: Proposed
**Difficulty**: Extreme
Executing highly sensitive logic inside isolated CPU physical memory enclaves. Guarantees that not even the host OS kernel can inspect the JavaScript memory heap during execution inside Intel SGX or ARM TrustZone.
