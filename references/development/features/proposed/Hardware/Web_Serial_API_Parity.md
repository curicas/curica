# Web Serial API Parity

**State**: Proposed
**Difficulty**: Medium
Directly exposing POSIX TTY interfaces (`/dev/ttyUSB0`) via the Web Serial API standard, empowering developers to build robust robotics and 3D printer controller logic purely in JavaScript.
