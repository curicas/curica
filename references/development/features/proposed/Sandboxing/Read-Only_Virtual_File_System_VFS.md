# Read-Only Virtual File System (VFS)

**State**: Proposed
**Difficulty**: High
Providing a highly restrictive abstraction layer where untrusted scripts are mounted into an ephemeral, in-memory file system. They can read injected mocks but cannot modify any host OS configurations.
