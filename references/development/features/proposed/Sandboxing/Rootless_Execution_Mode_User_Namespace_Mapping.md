# Rootless Execution Mode / User Namespace Mapping

**State**: Proposed
**Difficulty**: Extreme
Natively leveraging Linux user namespaces to drop all root privileges automatically upon startup. Curica maps itself to a pseudo-user securely, eliminating massive attack vectors without relying on Docker containers.
