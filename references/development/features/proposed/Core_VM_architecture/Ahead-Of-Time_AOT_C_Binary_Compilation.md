# Ahead-Of-Time (AOT) C Binary Compilation

**State**: Proposed
**Difficulty**: Extreme
Transpiling CBC bytecode into pure C source files, which are compiled by GCC and linked directly into the executable, converting JS applications into standalone native binaries with absolute maximum performance.
